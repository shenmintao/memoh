# Memoh development configuration
# Connects to devenv/docker-compose.yml infrastructure

[log]
level = "debug"
format = "text"

[server]
addr = ":8080"
rpc_listen_addr = ":9090"

[channel]
addr = ":8081"
rpc_listen_addr = ":9091"

[internal_rpc]
server_target = "server:9090"
channel_target = "channel:9091"
shared_secret = "memoh-dev-internal-rpc-secret"

[admin]
username = "admin"
password = "admin123"
email = "dev@memoh.local"

[auth]
jwt_secret = "memoh-dev-secret-do-not-use-in-production"
jwt_expires_in = "168h"
agent_credentials_encryption_key = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="

timezone = "UTC"

[database]
# Memoh now supports PostgreSQL only.
driver = "postgres"

[container]
# Development runs nested containerd inside the server container, matching the
# production Docker Compose image.
backend = "containerd"
# registry = "memoh.cn"  # Uncomment for China mainland mirror
default_image = "memohai/workspace:debian"
image_pull_policy = "if_not_present"
# containerd snapshotter and CNI settings for bot workspace containers.
snapshotter = "overlayfs"
data_root = "/opt/memoh/data"
bridge_path = "/opt/memoh/runtime/bridge"
cni_bin_dir = "/opt/cni/bin"
cni_conf_dir = "/etc/cni/net.d"

[containerd]
# Embedded containerd socket inside the dev server container.
socket_path = "/run/containerd/containerd.sock"
namespace = "default"

[docker]
# Used only when [container].backend = "docker"; the dev Compose stack uses containerd.
host = ""
# Dedicated user-defined network shared by a custom containerized
# Docker-backend server and its workspace containers. Requires
# [bridge_tls].mode = "strict".
network = ""
# Container ID or unique name of this Memoh server on the configured Docker
# daemon. Required together with network.
server_container = ""

[postgres]
host = "postgres"
port = 5432
user = "memoh"
password = "memoh123"
database = "memoh"
sslmode = "disable"

[pgvector]
enabled = true
host = "127.0.0.1"
port = 5433
user = "memoh"
password = "memoh123"
database = "memoh_vector"
sslmode = "disable"

[registry]
providers_dir = "conf/providers"

[supermarket]
base_url = "https://supermarket.memoh.ai"

[connect_it]
# The Connect-It dev service is available at http://localhost:18421.
# `mise run dev` shares one fixed bootstrap token between both sides: the
# Connect-It container seeds it at startup and the Memoh server presents it.
# If that token was revoked through the Connect-It admin UI, rotate it once:
#   MEMOH_CONNECT_IT_API_TOKEN=cit_<64 hex chars> mise run dev
# The new value is seeded as a fresh token; the revoked one stays revoked.
# These empty values are overridden by the dev Compose environment.
base_url = ""
api_token = ""

[oauth_clients]
config_path = "conf/oauth-clients.toml"

[web]
host = "0.0.0.0"
port = 8082
