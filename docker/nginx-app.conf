# Memoh Web 应用的 server 级共享配置：HTTP(8082) 与可选 HTTPS/h2 入口共同
# include 本文件，保证两个入口行为一致。经 envsubst 渲染到
# /etc/nginx/conf.d/includes/memoh-app.conf（conf.d 子目录不会被自动 include）。
root /usr/share/nginx/html;
index index.html;

client_max_body_size 50m;

# Gzip 压缩
gzip on;
gzip_vary on;
gzip_min_length 1024;
gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json application/javascript;

# 前端路由
location / {
    try_files $uri $uri/ /index.html;
}

# Nginx 健康检查
location = /health {
    access_log off;
    add_header Content-Type text/plain;
    return 200 "ok\n";
}

# 统一代理参数
proxy_http_version 1.1;
proxy_set_header Upgrade $http_upgrade;
proxy_set_header Connection $connection_upgrade;
proxy_set_header Host $host;
proxy_cache_bypass $http_upgrade;
proxy_set_header X-Real-IP $remote_addr;
proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Host $host;
proxy_set_header X-Forwarded-Proto $scheme;
proxy_connect_timeout 60s;
proxy_send_timeout 300s;
proxy_read_timeout 300s;

# Swagger 文档（保留 /api 前缀）
location ~ ^/api/(docs(?:/.*)?|swagger\.json)$ {
    proxy_pass http://${MEMOH_SERVER_UPSTREAM};
}

# Weixin QR login is implemented by the live Channel adapter.
location ~ ^/api/bots/[^/]+/channel/weixin/qr/(start|poll)$ {
    rewrite ^/api/(.*)$ /$1 break;
    proxy_pass http://${MEMOH_CHANNEL_UPSTREAM};
}

# SSE 长连接：关闭代理缓冲与 gzip、拉长读超时，保证事件即时下发。
# 后端所有 SSE 端点同时发送 X-Accel-Buffering: no，对未列入本 location 的
# 流式端点（如 POST /bots/stream、容器创建流）同样禁用缓冲。
location ~ ^/api/(bots/[^/]+/sessions(?:/[^/]+/messages)?/events|bots/[^/]+/web/stream)$ {
    proxy_pass http://${MEMOH_SERVER_UPSTREAM}/$1$is_args$args;
    proxy_buffering off;
    proxy_cache off;
    gzip off;
    proxy_read_timeout 1h;
    proxy_send_timeout 1h;
}

# API 代理（其余 /api/* 去掉 /api 前缀转发）
# Device webhooks may carry a scoped key in the URL. Avoid recording it in
# proxy logs, and never retry a notification POST against another upstream.
location ^~ /api/push/ {
    access_log off;
    error_log /dev/null crit;
    client_max_body_size 64k;
    proxy_next_upstream off;
    proxy_pass http://${MEMOH_SERVER_UPSTREAM}/push/;
}

location /api/ {
    proxy_pass http://${MEMOH_SERVER_UPSTREAM}/;
}

# Channel webhook proxy. Keep the full path intact.
location ^~ /channels/ {
    proxy_pass http://${MEMOH_CHANNEL_UPSTREAM};
}

# Email provider webhooks are owned by the Channel service.
location ^~ /email/mailgun/webhook/ {
    proxy_pass http://${MEMOH_CHANNEL_UPSTREAM};
}

# 静态资源缓存
location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}

# 安全头
add_header X-Frame-Options "SAMEORIGIN" always;
add_header X-Content-Type-Options "nosniff" always;
add_header X-XSS-Protection "1; mode=block" always;
